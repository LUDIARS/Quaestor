{
  "$comment": "Quaestor 統合設定 (AIFormat RULE.md §7.1)。非シークレットのみ。env は override 用で既定値はこのファイル。シークレットは `npm run secret -- set NAME VALUE` (app_data/secrets.enc.json、暗号化) へ。",
  "server": {
    "host": "127.0.0.1",
    "port": 17400,
    "logLevel": "info"
  },
  "storage": {
    "dbPath": "app_data/quaestor.db",
    "receiptsRoot": "app_data/receipts"
  },
  "mailIntake": {
    "$comment": "メール監視パートタイマー。認証情報は暗号化ストアの QUAESTOR_GMAIL_*。",
    "enabled": true,
    "query": "in:inbox newer_than:3d -category:promotions -category:social",
    "documentsRoot": "app_data/inbound",
    "maxAttachmentBytes": 15728640,
    "rules": [
      { "kind": "cloud_notice", "fromDomains": ["amazonaws.com", "amazon.com"], "subjectAny": ["Invoice", "Billing", "SES", "Support", "Production Access", "Your AWS account"] },
      { "kind": "cloud_notice", "fromDomains": ["google.com", "cloud.google.com"], "subjectAny": ["Invoice", "Billing", "budget", "Cloud"] },
      { "kind": "invoice", "attachmentMime": ["application/pdf"], "subjectAny": ["請求書", "請求", "インボイス", "Invoice", "領収書", "Receipt"] },
      { "kind": "invoice", "attachmentMime": ["application/pdf"], "fromDomains": ["freee.co.jp", "moneyforward.com", "misoca.jp", "invoice.ne.jp"] }
    ]
  },
  "ocrWorker": {
    "enabled": true,
    "intervalMs": 30000
  },
  "ocrClaudeCode": {
    "$comment": "レシート OCR を走らせる claude CLI の --model。CLI 既定に任せると対話セッションで使い切ったモデルの上限に巻き込まれる。null で CLI 既定へ委ねる。",
    "model": "sonnet"
  },
  "ocrSidecar": {
    "manage": true,
    "host": "127.0.0.1",
    "port": 17350,
    "lang": "japan",
    "python": null,
    "venvPython": null,
    "externalUrl": null
  },
  "training": {
    "gaRoot": "app_data/training/ga"
  },
  "invoiceShare": {
    "$comment": "請求書の公開マジックリンク (spec/feature/invoice-public-magic-link.md)。 publicUrl は Cloudflare 経由の HTTPS origin のみ (path/query/credential 不可)。 null の間はリンク発行が 503 で失敗する。 roots はこの PDF 以外を共有させないための許可ルート。",
    "publicUrl": "https://qs-magiclink.ai-run-do.com",
    "roots": ["data", "app_data/invoices"],
    "email": {
      "$comment": "請求書メールの送信元 (Amazon SES, spec/setup/config-and-secrets.md §3)。資格情報は暗号化ストアの QUAESTOR_SES_ACCESS_KEY_ID / QUAESTOR_SES_SECRET_ACCESS_KEY。",
      "region": "us-east-1",
      "fromAddress": "invoice@ai-run-do.com",
      "configurationSet": null
    }
  },
  "notifications": {
    "$comment": "アドバイザーのアドバイスを Discord webhook に定期通知。 webhook URL は `npm run secret -- set QUAESTOR_DISCORD_WEBHOOK_URL <url>` で登録。 subsidies は crawl+LLM が走るため既定 off (planId 指定で有効化)。",
    "enabled": false,
    "intervalMs": 21600000,
    "invest": true,
    "portfolio": true,
    "subsidies": { "enabled": false, "planId": null }
  },
  "web": {
    "$comment": "Vite dev server の allowedHosts (外部ホスト = Cloudflare Tunnel / Tailscale 等)。localhost / 127.0.0.1 は常に許可されるため不要。変更後は dev server 再起動が必要。設定 UI (/v1/config/web) からも変更可。",
    "allowedHosts": ["quaestor.vtn-game.com"]
  }
}
